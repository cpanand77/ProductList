_schema-version: "3.2"
ID: productmodule
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: productmodule-destination-content
  type: com.sap.application.content
  requires:
  - name: productmodule-destination-service
    parameters:
      content-target: true
  - name: productmodule-repo-host
    parameters:
      service-key:
        name: productmodule-repo-host-key
  - name: productmodule-uaa
    parameters:
      service-key:
        name: productmodule-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: productmodule_html_repo_host
          ServiceInstanceName: productmodule-html5-srv
          ServiceKeyName: productmodule-repo-host-key
          sap.cloud.service: productmodule
        - Authentication: OAuth2UserTokenExchange
          Name: productmodule_uaa
          ServiceInstanceName: productmodule-xsuaa-srv
          ServiceKeyName: productmodule-uaa-key
          sap.cloud.service: productmodule
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: productmodule-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: productmodule-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - productmodule.zip
      name: productmodule
      target-path: resources/
- name: productmodule
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: productmodule-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: productmodule-destination-service
    service-plan: lite
- name: productmodule-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: productmodule-xsuaa-srv
    service-plan: application
- name: productmodule-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: productmodule-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
